import { it } from 'mocha';
import { spec } from 'pactum';

describe('Pactum Exclusive Tests', () => {

    // Uncomment the Below Code to see the Exclusive Test Execution
    /*
    
    it('Should Run Only This Test Case', async() => {
        await spec().get('/exclusive').expectStatus(200);
    });

    */

});